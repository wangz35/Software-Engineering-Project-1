import Code as c
def uppercase_vowel():
    assert(c.vowel("A"))
def lowercase_vowel():
    assert(c.vowel("e"))
def uppercase_consonant():
    assert(c.vowel("b"))
def capital_consonant():
    assert(c.vowel("n"))

